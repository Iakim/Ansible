---
- hosts: Elasticsearch
  become: true
  become_user: root
  roles:

  # Pre
    #- initpack
    #- firewall
    #- epel
    - java

  # Combined
    - apache
    #- mysql

  # App
    # Zabbix
    #- zabbix-server
    #- zabbix-agent

    # Otrs
    #- otrs_install
    #- reboot
    #- otrs_start

    # ElasticSearch + ElasticHQ
    - elasticsearch
    - elastichq
